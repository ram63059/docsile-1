module.exports = {
  theme: {
    extend: {
      // ... other extensions
    },
  },
  variants: {},
  plugins: [
    require('tailwind-scrollbar-hide')  // Make sure this plugin is installed
  ],
} 